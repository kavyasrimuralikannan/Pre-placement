from collections import Counter

X = int(input())

sizes = list(map(int, input().split()))

stock = Counter(sizes)

N = int(input())

money = 0

for i in range(N):
    size, price = map(int, input().split())
    
    if stock[size] > 0:
        money += price
        stock[size] -= 1

print(money)
