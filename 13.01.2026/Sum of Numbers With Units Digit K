class Solution {
    public int minimumNumbers(int num, int k) {
        if(num == 0)return 0;
        int i = 1;
        while(i*k <= num && i <= 10){
            if((k*i)%10 == num%10) return i;
            i++;
        }
        return -1;
    }
}
